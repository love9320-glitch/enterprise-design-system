# Figma \"테스트 디자인 시스템\" 파일에 코드-투-디자인으로 그릴 때의 파일키·라이브러리·폰트·변수 제약

> 이 파일은 저장소가 원본입니다(2026-08-10 노트북 메모리에서 이관 — 집/회사 어느 컴퓨터에서든 공유).
> 내용을 갱신하면 저장소에 커밋할 것. 개인 메모리에는 다시 쌓지 않는다.

코드베이스 디자인 시스템을 Figma에 그릴 때 쓰는 대상 파일/라이브러리와 환경 제약.

- **Figma 파일키**: `h9jZFkEHfcHUGok1TZjjlP` ("테스트 디자인 시스템"). `modal` 페이지 nodeId `7601:47210`.
- **퍼블리시 라이브러리**: "design system test" (libraryKey `lk-d21521ae7508f4d94f51f653bf9de62e4100bcee7f3bfdca69650188ffa60f08fa26b65efb3636f927eda018aed30a32fff41a64cecd10517b84d5c26209c1c4`). Button(primary/line/bg/ghost × text/icon), `modal / confirm`, `modal / components / input_selection header`(타이틀+line/x 닫기 내장), `table / table`(※ "Component set has existing errors"라 인스턴스 사용 불가 → 표는 수동 조립) 등이 있음. 버튼 라벨은 중첩 `Button / components/ base button`의 TEXT prop `button name#32:4` 또는 `Button Text` 노드.
- **폰트(2026-06-24 Pretendard 설치 완료)**: 디자인 시스템 폰트 **Pretendard가 이제 Figma MCP 환경에 설치됨** — `listAvailableFontsAsync`에 `Pretendard`(Thin~Black 9스타일) 노출, `loadFontAsync` 성공. **텍스트 편집 시 Pretendard로 정상 편집 가능**(텍스트 노드는 `getStyledTextSegments(['fontName'])`로 세그먼트 폰트 로드 후 `characters` 설정 → 기존 Pretendard 굵기 유지). 컴포넌트가 깨지지 않음을 확인. 따라서 **텍스트 편집 보류는 더 이상 기본이 아님** — 과거 독립 '폰트 정책' 규칙(구 규칙 14)은 2026-06-24 폐지되어 **규칙 4(요소 분석)의 '폰트 가용성 점검'으로 통합**(폰트 없고 설치도 불가한 환경에서만 텍스트 변경 보류). 단 **버튼 등 중첩 인스턴스의 text는 `setProperties`로는 여전히 실패**("node not found", 폰트 무관·구조 문제) → 버튼 텍스트 변경은 규칙 5 패턴(state=variant 적용 후 `detachInstance()` → 텍스트 편집)으로 처리.
- **Button Code Connect 매핑 완료**: 코드 `Button`(`design-system/src/components/Button.jsx`) ↔ Figma 버튼 컴포넌트 세트 15개(fill/line/ghost 각 text·left·right·icon = 4, underline는 icon 없이 3) 매핑됨(`add_code_connect_map`, 템플릿으로 variant/size/state→disabled·loading 변환). Code Connect는 **게시(publish)된 컴포넌트에만** 등록 가능. `state` Default/Hover/Pressed→일반, Disabled→`disabled`, Loading→`loading`. 아이콘은 Figma `image` placeholder → 코드 `leftIcon/rightIcon/icon={Icon}` placeholder. 테스트용 빈 페이지 `test button` nodeId `7626:2414`.
- **변수 import 제약**: `distance/*`·`round/*`(FLOAT), `table/*`·`*button color/*`(COLOR)는 `importVariableByKeyAsync`로 import 가능. 하지만 `font_icon 5 (main)`·`color/font_icon/font_icon 4` 등 코어 폰트색 변수는 key import 실패 → **기존 인스턴스(예: 헤더)의 boundVariables에서 `getVariableByIdAsync`로 Variable 객체를 가져와 재사용**.
- **로컬 버튼 컴포넌트 위치**: `01_buttons` 페이지(pageId `943:36646`)에 fill/line/ghost/underline 4종 × (text·left icon·right icon·icon) 컴포넌트 세트가 로컬로 존재. fill 세트 = `01_fill_text button`(7045:203781), `01_fill_left icon text button`(7051:6), `01_fill_right icon text button`(7051:974), `01_fill_icon button`(7054:62). 각 세트는 state(Default/Hover/Pressed/Disabled/Loading) × size(32/24) = 10 variant COMPONENT. 인스턴스는 라이브러리 import 말고 variant COMPONENT id로 `getNodeByIdAsync→createInstance`. (코드 Button variant: fill=primary(black)/line/ghost/underline) [[figma-use-local-components]]
- `test` 페이지(pageId `7626:3255`)에 작업용 빈 Frame들이 있음(예: `7850:13281`, `7850:14086`). 버튼 텍스트 기본값은 "다음 단계".
- **컴포넌트 탐색은 Code Connect로 먼저(빠름).** 코드↔Figma가 Code Connect로 연결돼 있으므로, `search_design_system` 반복·`use_figma` 페이지 순회로 더듬지 말 것. ① 그릴 화면/노드의 `get_code_connect_map(fileKey,nodeId)` → 그 하위 노드가 각각 어떤 코드 컴포넌트(`componentName`+`source`)인지 한 번에(예: `fill type button`(7057:6710) 하위 40노드 전부 `Button.jsx`). ② 컴포넌트 props·variant·하위 트리는 `get_context_for_code_connect(fileKey,nodeId)`로 한 번에(임시 인스턴스 만들어 읽을 필요 없음). (현재 Button 매핑은 `hasTemplate:false` — 노드↔컴포넌트 연결만, props 템플릿은 없음)
- 작업 절차는 [[feedback_design_system_rules]] 및 figma-use/figma-generate-design 스킬을 따른다. **단, 라이브러리 import보다 로컬 컴포넌트 우선** ([[figma-use-local-components]]).

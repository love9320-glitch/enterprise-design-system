// page header(페이지 타이틀 영역) 시멘틱 컬러 토큰
// Figma file: h9jZFkEHfcHUGok1TZjjlP — "page header" (node 9216:12647)
//   heading/* 변수를 baseColors 경유로 정의한다.
import { baseColors } from '../colors';

const b = baseColors.base;

export const headingColors = {
  bg: b.white, // #ffffff — 페이지 헤더 배경(Figma heading/bg)
};
